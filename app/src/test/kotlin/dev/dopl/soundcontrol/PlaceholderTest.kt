package dev.dopl.soundcontrol

import org.junit.Assert.assertEquals
import org.junit.Test

class PlaceholderTest {

    @Test
    fun `test pipeline is wired`() {
        assertEquals(4, 2 + 2)
    }
}
